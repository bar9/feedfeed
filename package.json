{
  "name": "feedfeed",
  "version": "1.0.0",
  "description": "A configurable polling feed reader",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/.bin/esbuild src/app.tsx --bundle --minify --sourcemap --outfile=./static/out.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "antd": "^4.20.7",
    "esbuild": "^0.14.42",
    "react": "^18.1.0",
    "react-dom": "^18.1.0"
  }
}
